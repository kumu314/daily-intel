# 部署说明：把情报日报变成手机桌面 App

成品是一个 **PWA（渐进式网页应用）**：零成本、不用上架商店，手机浏览器「添加到主屏幕」后就跟 App 一样，还能离线看已打开过的那天。

站点目录：`intel/site/`（自包含，直接推 GitHub Pages 即可）。

- `index.html` —— 每日列表仪表盘（最新在上）
- `<日期>.html` —— 每一天日报（已带 PWA 头 + 离线缓存）
- `manifest.webmanifest` / `sw.js` / `icon.png` —— 安装与离线用

---

## 一、今晚本地预览（不用 GitHub 也能看）

在 `intel/site` 目录下起个本地服务：

```bash
cd intel/site
python -m http.server 8000
```

手机和电脑连同一个 WiFi，手机浏览器打开 `http://<电脑局域网IP>:8000`。
电脑 IP 用 `ipconfig` 查（看「IPv4 地址」那一串 192.168.x.x）。

> 注意：本地 http 只能「看」，浏览器不会弹「安装到桌面」——安装提示要 HTTPS。所以今晚先验证内容和排版，装桌面等下面 GitHub Pages 上线后用 https 打开即可。

---

## 二、正式部署到 GitHub Pages（公开，当 IP 入口）

1. 在 GitHub 新建一个**公开**仓库，比如 `daily-intel`。
2. 把整个 `intel/` 目录（含 `site/`、`data/`、`*.py`、`intel.db`）推上去。
   GitHub Pages 源设成：**main 分支 → /site 目录**（或 gh-pages 分支根目录）。
3. 等一两分钟，访问 `https://<你的用户名>.github.io/daily-intel/`。
4. 用手机 https 打开这个地址 → 浏览器菜单「添加到主屏幕」→ 桌面出现「枯木每日情报」图标。

以后每天只要 `site/` 被更新并推送，手机刷新就是最新。

---

## 三、全自动每周更新（你定的节奏）

每周让 agent 跑一遍「采集 → 入库 → 出报告 → 出站点 → 推送」，你零操作。
每周一次比每天更省心，也留出"等实践验证"的空隙，和每月 1 号「验尸总扫」错开节奏。

流水线脚本已齐：
- `report.py` 出单日 HTML
- `build_site.py` 把 `data/*.json` 全渲染成 `site/` 并重建 `index.html`
- `ingest.py` 把 `data/YYYY-MM-DD.json` 入库

**每周自动化的 prompt（交给 WorkBuddy 定时任务，建议每周日 21:00）：**

```
你是一手情报周报的"小编"。请生成本周的周报并推送到 GitHub Pages 仓库。

1. 联网采集本周的一手信息（只认一手/官方源：国务院、国家统计局、央行、工信部、
   美联储、BLS/BEA、IEA、交易所行情等），按现有 data/2026-08-31.json 的 schema
   写一份 data/YYYY-MM-DD.json（YYYY-MM-DD 用本周最后一天，即今天）：
   - facts（每条带 dim/source/tier/level，level:1必须知道/2值得知道/3存档）
   - metrics（KPI 用：LPR/联邦基金/核心CPI/核心PCE/加息概率 等真实数字）
   - analysis.cross 每条判断必须写 falsify（翻车线：什么情况出现说明我错了 + due 到期日）
   - analysis.red_ocean / blue_ocean（heat 是主观估计，必须标"这是估计不是数据"）
   - analysis.playbook（给枯木的落点）、watchlist、headline
   遵守三条纪律：不同性质数字不同图；主观打分不当数据；每条判断有翻车线。
   文风：短句、大白话、删官方定性形容词。

2. 跑：python intel/ingest.py  →  python intel/report.py  →  python intel/build_site.py
   确认 site/ 下生成了本周 <日期>.html 且 index.html 已更新。

3. 把 intel/site/ 下所有文件 + intel/data/<今天>.json + intel/db/intel.db
   通过 GitHub MCP（create_or_update_file / push_files）推到仓库 <用户名>/daily-intel
   的 main 分支（Pages 源为 /site）。

4. 发一条极简汇报：本周记了 N 条事实、M 条判断、 headline 一句话。
   若某关键一手数据本周没发布，明说"本周无新增官方数据"，不要编造。

纪律：宁可少写，绝不编数。翻车了以后在验尸里明说。
```

> 头几周建议枯木自己扫一眼，确认质量稳定后再完全放手。质量漂移由每月 1 号的「验尸总扫」兜底核对。

---

## 四、先自己后开放

现在仓库可以先不宣传。等日报质量跑顺（建议观察 2–4 周），再把地址发到小红书/ B站
当「内向友好的一手情报站」IP 入口——这本身就是「边学 Python 边教数据分析」的活样板。
